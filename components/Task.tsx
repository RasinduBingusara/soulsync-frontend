class Task {
  constructor(id:number, title:string, description:string, 
    date:string, time:string, category:string, suggestion:string) {
    id = id;
    title = title;
    description = description;
    date = date;
    time = time;
    category = category;
    suggestion = suggestion;
  }
}